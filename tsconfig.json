{
  // This config is used only for the linter (ts-standand)
  "extends": "@tsconfig/node-lts-strictest-esm/tsconfig.json",
  "exclude": ["commitlint.config.js", "packages/*/dist", "doc", "packages/*/*.js", "plop-templates", "plopfile.mjs"]
}